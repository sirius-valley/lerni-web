export enum ComponentVariantType {
  DARK = 'dark',
  PRIMARY = 'primary',
  RED = 'red',
  GHOST = 'ghost',
}
export enum TextInputStatus {
  DEFAULT = 'default',
  PLACEHOLDER = 'placeholder',
  ERROR = 'error',
  DISABLED = 'disabled',
}
